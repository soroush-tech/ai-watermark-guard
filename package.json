{
  "name": "ai-watermark-guard-repo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test": "vitest run"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^3.2.4"
  }
}
